[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
flake8 = "*"
black = "*"
ipython = "*"
isort = "*"

[packages]
colorama = "*"

[requires]
python_version = "3.7"

[scripts]
lint = "bash -c 'flake8 . && black --check . && isort -rc -c -df .'"
fix = "bash -c 'isort -rc . && black .'"

[pipenv]
allow_prereleases = true
